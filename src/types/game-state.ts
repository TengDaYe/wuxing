/**
 * 游戏状态类型定义
 */

/**
 * 游戏状态枚举
 */
export enum GameState {
  GAME_START = 'start',
  GAME_PLAYING = 'playing',
  GAME_PAUSE = 'paused',
  GAME_END = 'end',
  GAME_GAME_OVER = 'game_over',
  GAME_EXIT = 'exit',
}
